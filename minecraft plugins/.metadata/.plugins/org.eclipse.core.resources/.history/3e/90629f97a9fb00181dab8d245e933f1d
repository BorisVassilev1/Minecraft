package org.CDnoMlqko.ChatUtils.enchantments;

import org.bukkit.enchantments.Enchantment;

public enum CustomEnchantmentRarity {
	
	COMMON(
			new String[]   {},
			new float[] {}),
	EPIC(
			new String[]   {},
			new float[] {}),
	LEGENDARY(
			new String[]   {},
			new float[] {});
	
	Enchantment[] enchantmentsPoll;
	float[] rarities;
	
	private CustomEnchantmentRarity(String[] names, float[] rarities) {
		this.enchantmentsPoll = new Enchantment[ids.length];
		for(int i = 0; i < ids.length; i ++)
		{
			enchantmentsPoll[i] = Enchantment.getByName(ids);
		}
	}

}
